server:
  port: 9000

spring:
  # mail send setting
  mail:
    host: smtp.naver.com
    port: 465
    username: dj9099@naver.com    # github 유출 금지
    password: NY1UUYXTV15Y        # github 유출 금지
    protocol: smtp
    properties:
        mail:
            smtp:
                auth: true
                ssl.enable: true


  # database setting
  datasource:
    url: jdbc:mariadb://localhost:3306/eventdb
    username: root
    password: mariadb
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    # DBMS dialect setting
    database-platform: org.hibernate.dialect.MariaDB106Dialect
    hibernate:
      # ddl
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true # SQL log
    database: mysql

# log level setting
logging:
  level:
    root: info
    com.study.event: debug
    org.hibernate.SQL: debug




